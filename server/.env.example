DATABASE_URL="postgresql://elmejormenu:elmejormenu@localhost:5432/elmejormenu?schema=public"
JWT_SECRET="change-this-to-a-long-random-string"
ANTHROPIC_API_KEY="sk-ant-..."
PORT=4000

# Comma-separated list of allowed CORS origins for the web app.
# Defaults to the production Expo web app + local dev servers if not set.
CORS_ORIGIN="https://el-mejor-menu--qqrfgmmftw.expo.app,http://localhost:8081,http://localhost:19006"
